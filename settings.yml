use_default_settings: true

server:
  secret_key: "${SEARXNG_SECRET}"
  limiter: false
  image_proxy: false
  allowed_hosts: "*"       

search:
  formats:
    - html
    - json

outgoing:
  request_timeout: 15.0
  max_request_timeout: 30.0
